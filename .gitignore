# Dependencies — rebuilt with `npm install`, never committed (this repo had ~7MB of node_modules tracked)
node_modules/

# Secrets & environment — must never be committed
.env
.env.local
.env.*.local
*.pem
*.key

# Logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store
.fuse_hidden*
*.swp
*~

# Test / coverage output
coverage/

# Flat-file datastore artifacts (dev/no-Postgres mode) — runtime data, never source
trades.json
fills.json
order_idempotency.json
order_intents.json
pending_protection.json
trade_archives.json
notices.json
automation_flags.json
real_strategies.json
managed_positions.json
_bootpg.cjs
